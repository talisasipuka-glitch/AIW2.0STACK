# Agent: uplift-agent (Stage 10.3)

## Role

Apply final niche polish: the chevron shape motif with restraint, verify
the restrained motion preset, and verify `tabular-nums` on every dollar
figure across the homepage and all case-type pages.

## Prerequisites

Stage 10.2 (`10-personalize.md`) has completed.

## Inputs

- `clients/[Client Name]/[Client Name] Website/dist/`
- `templates/personal-injury-lawyers/niche-playbook/`
- `.claude/sops/11-uplift.sop.md`

## Steps

### Step 0, Read accumulated lessons (universal)

Read `.claude/lessons/by-agent/11-uplift.md` and any per-client lessons at
`clients/[Client Name]/Pipeline Data/lessons/notes.md`. Apply every rule as
an override to the spec below.

### Step 1, Apply chevron motif and verify motion

Follow `.claude/sops/11-uplift.sop.md` Procedure steps 1 to 2: apply the
chevron motif at hero/CTA corner overlays (0.08 opacity), section dividers,
and PracticeAreasGrid/StatsBar card edges, with restraint. Verify
`--motion-duration: 600ms` / `--motion-stagger: 90ms` consistency and
`prefers-reduced-motion`.

### Step 2, Verify tabular-nums and palette restraint

Follow steps 3 to 5: run a tabular-nums verification pass on every dollar
figure (homepage StatsBar, case-type single-figure StatsBar, any
testimonial/grid dollar mentions including `settlementFigure` values);
verify yellow accent restraint across PracticeAreasGrid, StatsBar, and
CTABand; verify light grey/white section alternation.

## Pass criteria

Per `.claude/sops/11-uplift.sop.md` Pass criteria: chevron motif present at
specified locations with restraint; motion preset consistent and
reduced-motion honored; every dollar figure on every page has
`tabular-nums`; yellow accent restraint holds; section alternation uses
light grey/white.

## When this agent halts

Per `.claude/sops/11-uplift.sop.md` When this stage halts: any dollar figure
anywhere lacks `tabular-nums` (hard requirement); chevron motif or yellow
accent over-applied past the restraint requirement.

## Outputs

Updated `clients/[Client Name]/[Client Name] Website/dist/` with uplift
applied. Hands off to `design-fidelity-qa-agent.md`.

---

## Author + version

Generated by Module 2D at `2026-06-14` from:

- Niche: `personal-injury-lawyers`
- Wireframe: `research/02-niche-research/personal-injury-lawyers/09-wireframe.md`
- Playbook: `templates/personal-injury-lawyers/niche-playbook/`
