# SOP Compliance Checklist (per-niche layer: Personal Injury Lawyers)

GENERATED by Module 2D from `research/02-niche-research/personal-injury-lawyers/09-wireframe.md`,
`09-template-spec.md`, and `09-sitemap.json`, plus the niche template's actual
components. The factory's sop-qa-agent loads this per-niche layer plus the
universal layer at `website-factory/.claude/checklists/sop-compliance.universal.md`
(when present) to compute the merged pass rate.

Score format: each item is `[ ] id, description` with a `Fix:` line for
automated remediation guidance.

Gate: 95% (PASS / (PASS + FAIL), excluding N/A) on the merged checklist.

---

## GLOBAL CHECKS

These checks apply to every page and every component in the build.

- [ ] `global.em_dash_audit`, `grep -rn` for em-dash + `&mdash;` +
  `&#8212;` across `src/` returns 0. Zero em-dashes anywhere in rendered
  output, per `website-factory/references/copy/typographic-standards.md`
  section 2.
  Fix: Replace with periods, commas, or colons.

- [ ] `global.ai_vocab_blocklist`, no rendered copy contains a term or
  phrase from `website-factory/references/copy/ai-vocab-blocklist.md` or
  `templates/personal-injury-lawyers/niche-playbook/copy-blocklist-additions.md`
  (additions file written in parallel by Phase 8; check both lists).
  Fix: Rewrite the offending sentence in plain language with a specific
  claim or concrete detail, per the blocklist's "Acceptable exceptions"
  section.

- [ ] `global.no_placeholders`, no surviving `__REQUIRED__*__` sentinel
  strings anywhere in a real client build, and no placeholder filenames in
  `public/`.
  Fix: Stage 4 / Stage 9 / manual drop required.

- [ ] `global.cta_anchors_call_now`, every "CALL NOW" element (header
  sticky CTA, footer footerCta, CTABand buttons where applicable) links to
  `brandDNA.contact.phoneTelLink`.
  Fix: Inspect `src/components/Header.jsx` and any CTABand instances;
  ensure the `href` resolves to the canonical `contact.phoneTelLink` path,
  not a hardcoded number.

- [ ] `global.cta_anchors_form`, every primary on-page CTA that is not a
  phone link anchors to the on-page `LeadForm` (`#lead-form`) or routes to
  `/contact`, per the wireframe's CTA pattern.
  Fix: Inspect the niche wireframe's CTA pattern and adjust the component.

- [ ] `global.real_logo_present`, client logo is a real SVG/PNG.
  Fix: Stage 4 asset-scraper / manual drop.

- [ ] `global.font_loaded`, Plus Jakarta Sans (heading) and Inter (body)
  load via Google Fonts per `brand-dna.typography.headingFontUrl` /
  `brand-dna.typography.bodyFontUrl`, and both are present in
  `src/index.css`.
  Fix: Verify `index.html` `<link rel="preconnect">` and the `@import
  url(...)` in `src/index.css` after `inject-theme.mjs` runs.

- [ ] `global.tabular_nums_on_figures`, settlement dollar figures and stat
  numbers (StatsBar, PracticeAreasGrid cards, CaseTypeHero) use
  `tabular-nums`.
  Fix: Apply the `tabular-nums` Tailwind utility to the figure's container.

- [ ] `global.lighthouse_mobile_score`, Lighthouse mobile performance >=
  80 (informational; flags but does not halt by itself).
  Fix: `tools/optimise-image.py`, lazy-load, code-split.

- [ ] `global.trust_stack_order`, the trust stack order from
  `09-template-spec.md` Section 7 holds across the site: (1) no win, no
  fee / contingency guarantee near every CTA, (2) settlement-dollar proof
  tagged by case type, (3) press / "as seen in" or local award badges, (4)
  risk-removal "why choose us" bullets, (5) case results grid with dollar
  figures, (6) named attorney story and photo, (7) testimonials with
  photos tied to case type and outcome, (8) FAQ (PAA-driven), (9) office
  locations (multi-office firms only, defaults to `[]` and the section is
  omitted for single-office clients).
  Fix: Inspect `src/pages/HomePage.jsx` section order against
  `09-template-spec.md` Section 4 and Section 7.

---

## HEADER (GLOBAL)

- [ ] `header.composition_matches_wireframe`, the Header renders the
  layout defined in `09-wireframe.md` "Global: Header + Footer": logo
  left, practice-area mega-nav dropdown center, "OPEN 24/7" badge plus
  "CALL NOW" phone link right, all visible on every scroll position.
  Fix: Inspect `src/components/Header.jsx` against the wireframe spec.

- [ ] `header.brand_dna_paths_canonical`, the Header reads only
  `company.name`, `company.shortName`, `contact.phone`,
  `contact.phoneTelLink`, `copy.topBar.cta`, `copy.availableNow`,
  `services[]`, `hours.emergencyBadge`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `header.copy_from_playbook`, locked copy strings (`copy.topBar.cta`,
  `copy.availableNow`) resolve from
  `templates/personal-injury-lawyers/niche-playbook/copy-locks.json`.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `header.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

- [ ] `header.single_persistent_call_now`, the header uses ONE persistent
  sticky "CALL NOW" instance, not repeated across multiple nav dropdowns
  (deviation from the Morgan & Morgan reference, which repeats "CALL NOW"
  8+ times).
  Fix: Inspect `src/components/Header.jsx`; remove any duplicate CALL NOW
  instances beyond the single sticky element.

---

## FOOTER (GLOBAL)

- [ ] `footer.composition_matches_wireframe`, the Footer renders the
  layout defined in `09-wireframe.md`: a repeated 4-field LeadForm
  (variant="footer") above a 3-column band (hours/badges, bar links plus
  license, copyright/credit).
  Fix: Inspect `src/components/Footer.jsx` against the wireframe spec.

- [ ] `footer.brand_dna_paths_canonical`, the Footer reads only
  `copy.formHeader`, `copy.formSubtext`, `copy.submitButton`,
  `copy.privacyLine`, `hours.display`, `company.licenseNumber`,
  `social.*`, `copy.footerCta`, `copy.copyright`, `credit.agency`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `footer.copy_from_playbook`, locked copy strings resolve from
  `templates/personal-injury-lawyers/niche-playbook/copy-locks.json`.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `footer.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

---

## HOME PAGE SECTIONS (`/`)

Per `09-template-spec.md` Section 4, in order: Hero, ProcessSteps, StatsBar,
PressBand, WhyChooseUs, PracticeAreasGrid, FounderStory, Testimonials,
FAQSection, CTABand.

### Hero

- [ ] `hero.composition_matches_wireframe`, Hero renders the layout
  defined in `09-wireframe.md` Page: Home section: split, copy plus form
  left/center, attorney image right, stacks on mobile with image first;
  trust chips above H1; LeadForm variant="hero" with 4 fields; sticky-form
  on mobile scroll.
  Fix: Inspect `templates/personal-injury-lawyers/src/components/Hero.jsx`
  against the wireframe spec.

- [ ] `hero.brand_dna_paths_canonical`, Hero reads only `copy.hero.*`,
  `copy.heroTrustChips[]`, `team.founder`, `team_group_photo`,
  `copy.formHeader`, `copy.formSubtext`, `copy.buttonText`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `hero.copy_from_playbook`, locked copy strings (H1 headline,
  subheadline "No Win, No Fee. Guaranteed. Available 24/7.", trust chips)
  resolve from `niche-playbook/copy-locks.json`.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `hero.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

### ProcessSteps

- [ ] `processsteps.composition_matches_wireframe`, ProcessSteps renders
  the layout defined in `09-wireframe.md`: 3-column on desktop, stacked on
  mobile, numbered badges, placed directly below the Hero before any
  stats.
  Fix: Inspect `src/components/ProcessSteps.jsx` against the wireframe
  spec.

- [ ] `processsteps.brand_dna_paths_canonical`, ProcessSteps reads only
  `process_steps[]` and `copy.process.*`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `processsteps.copy_from_playbook`, locked copy strings resolve from
  `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `processsteps.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

### StatsBar

- [ ] `statsbar.composition_matches_wireframe`, StatsBar renders the
  layout defined in `09-wireframe.md`: horizontal scroll-snap grid of 4 or
  more settlement dollar figures, case-type-tagged, accent-yellow,
  tabular-nums.
  Fix: Inspect `src/components/StatsBar.jsx` against the wireframe spec.

- [ ] `statsbar.brand_dna_paths_canonical`, StatsBar reads only
  `services[]` and `pages.services.items[].settlementFigure`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `statsbar.copy_from_playbook`, locked copy strings resolve from
  `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `statsbar.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

- [ ] `statsbar.minimum_four_figures`, the home page StatsBar renders at
  least 4 settlement dollar figures (Dolman density requirement).
  Fix: Verify `brandDNA.pages.services.items[]` has settlement figures
  populated for at least 4 case types.

### PressBand

- [ ] `pressband.composition_matches_wireframe`, PressBand renders the
  layout defined in `09-wireframe.md`: full-width logo strip, greyscale
  logos, falls back to `trust_badges[]` when `press_logos[]` is empty.
  Fix: Inspect `src/components/PressBand.jsx` against the wireframe spec.

- [ ] `pressband.brand_dna_paths_canonical`, PressBand reads only
  `press_logos[]` and `trust_badges[]`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `pressband.copy_from_playbook`, locked copy strings resolve from
  `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `pressband.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

- [ ] `pressband.fallback_renders`, when `press_logos[]` is empty, the
  section renders `trust_badges[]` instead of an empty band.
  Fix: Inspect the PressBand component's conditional render logic.

### WhyChooseUs

- [ ] `whychooseus.composition_matches_wireframe`, WhyChooseUs renders the
  layout defined in `09-wireframe.md`: 2x2 (desktop) / 1-column (mobile)
  bullet grid with a chevron icon per bullet, risk-removal framing.
  Fix: Inspect `src/components/WhyChooseUs.jsx` against the wireframe
  spec.

- [ ] `whychooseus.brand_dna_paths_canonical`, WhyChooseUs reads only
  `why_choose_us[]` and `copy.whyChoose.*`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `whychooseus.copy_from_playbook`, locked copy strings resolve from
  `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `whychooseus.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

### PracticeAreasGrid

- [ ] `practiceareasgrid.composition_matches_wireframe`,
  PracticeAreasGrid renders the layout defined in `09-wireframe.md`: 4-col
  (desktop) / 2-col (tablet) / 1-col (mobile) card grid, 7 cards, each
  showing case type name, dollar result, one-line description, linking to
  `/case-types/{slug}`.
  Fix: Inspect `src/components/PracticeAreasGrid.jsx` against the
  wireframe spec.

- [ ] `practiceareasgrid.brand_dna_paths_canonical`, PracticeAreasGrid
  reads only `services[]`, `pages.services.items[]`, and
  `copy.services.*`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `practiceareasgrid.copy_from_playbook`, locked copy strings resolve
  from `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `practiceareasgrid.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

- [ ] `practiceareasgrid.seven_cards_present`, the grid renders exactly 7
  cards: car accident, truck accident, motorcycle accident, slip and fall,
  wrongful death, dog bite, brain injury.
  Fix: Verify `brandDNA.services[]` and
  `brandDNA.pages.services.items[]` contain all 7 entries.

### FounderStory

- [ ] `founderstory.composition_matches_wireframe`, FounderStory renders
  the layout defined in `09-wireframe.md`: image left, copy right
  (reverses on mobile: image first), named attorney photo plus 2-paragraph
  origin story.
  Fix: Inspect `src/components/FounderStory.jsx` against the wireframe
  spec.

- [ ] `founderstory.brand_dna_paths_canonical`, FounderStory reads only
  `team.founder` and `copy.founder.*`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `founderstory.copy_from_playbook`, locked copy strings resolve from
  `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `founderstory.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

- [ ] `founderstory.real_attorney_photo`, the founder photo is a real
  photo of the named attorney, not a gavel or scales-of-justice stock
  image (explicit anti-pattern per `08-starter-template.md`).
  Fix: Stage 4 asset-scraper / Stage 9 hero image / manual drop.

### Testimonials

- [ ] `testimonials.composition_matches_wireframe`, Testimonials renders
  the layout defined in `09-wireframe.md`: 3-card carousel (desktop) /
  single-card swipe (mobile), each card with photo, name, case type,
  outcome.
  Fix: Inspect `src/components/Testimonials.jsx` against the wireframe
  spec.

- [ ] `testimonials.brand_dna_paths_canonical`, Testimonials reads only
  `reviews.items[]`, `reviews.rating`, `reviews.googleLabel`, and
  `copy.reviews.*`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `testimonials.copy_from_playbook`, locked copy strings resolve from
  `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `testimonials.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

### FAQSection

- [ ] `faqsection.composition_matches_wireframe`, FAQSection renders the
  layout defined in `09-wireframe.md`: accordion, 5 to 8 items, PAA-driven,
  plain language.
  Fix: Inspect `src/components/FAQSection.jsx` against the wireframe spec.

- [ ] `faqsection.brand_dna_paths_canonical`, FAQSection reads only
  `faq[]` and `copy.faq.*`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `faqsection.copy_from_playbook`, locked copy strings resolve from
  `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `faqsection.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

- [ ] `faqsection.schema_present`, the FAQ section emits `FAQPage`
  JSON-LD schema.
  Fix: Verify `inject-theme.mjs` injects `FAQPage` schema for this
  section's `faq[]` entries.

### CTABand

- [ ] `ctaband.composition_matches_wireframe`, CTABand renders the layout
  defined in `09-wireframe.md`: full-width band, accent-yellow background,
  centered copy plus button, final conversion push before the footer.
  Fix: Inspect `src/components/CTABand.jsx` against the wireframe spec.

- [ ] `ctaband.brand_dna_paths_canonical`, CTABand reads only `copy.cta.*`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `ctaband.copy_from_playbook`, locked copy strings resolve from
  `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `ctaband.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

---

## CASE-TYPE LANDING PAGE SECTIONS (`/case-types/:slug`)

Per `09-template-spec.md` Section 5, one component `CaseTypeLandingPage`
rendered for all 7 routes, in order: CaseTypeHero, RiskRemovalBand, StatsBar,
CaseTypeExplainer, FAQSection, CTABand + LeadForm.

### CaseTypeHero

- [ ] `casetypehero.composition_matches_wireframe`, CaseTypeHero renders
  the layout defined in `09-wireframe.md`: centered, full-width band, H1 =
  "[Case Type] Lawyer in [City] | [Firm Name]", settlement figure for this
  case type prominent, accent-yellow, tabular-nums.
  Fix: Inspect `src/components/CaseTypeHero.jsx` against the wireframe
  spec.

- [ ] `casetypehero.brand_dna_paths_canonical`, CaseTypeHero reads only
  `company.name`, `address.city`, `pages.services.items[slug].name`, and
  `pages.services.items[slug].settlementFigure`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `casetypehero.copy_from_playbook`, locked copy strings (H1 pattern)
  resolve from `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `casetypehero.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

### RiskRemovalBand

- [ ] `riskremovalband.composition_matches_wireframe`, RiskRemovalBand
  renders the layout defined in `09-wireframe.md`: thin full-width strip
  restating contingency plus 24/7 availability from `copy.trustClaims[]`.
  Fix: Inspect `src/components/RiskRemovalBand.jsx` against the wireframe
  spec.

- [ ] `riskremovalband.brand_dna_paths_canonical`, RiskRemovalBand reads
  only `copy.trustClaims[]`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `riskremovalband.copy_from_playbook`, locked copy strings
  ("No win, no fee" plus 24/7 restatement) resolve from
  `niche-playbook/copy-locks.json`.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `riskremovalband.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

### StatsBar (single-figure variant)

- [ ] `statsbar_single.composition_matches_wireframe`, the single-figure
  StatsBar variant renders the layout defined in `09-wireframe.md`: one
  large stat card, centered, reusing the StatsBar component with
  `items.length === 1`.
  Fix: Inspect `src/components/StatsBar.jsx`'s single-item render path
  against the wireframe spec.

- [ ] `statsbar_single.brand_dna_paths_canonical`, the single-figure
  StatsBar reads only `pages.services.items[slug].settlementFigure`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `statsbar_single.copy_from_playbook`, locked copy strings resolve
  from `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `statsbar_single.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

### CaseTypeExplainer

- [ ] `casetypeexplainer.composition_matches_wireframe`,
  CaseTypeExplainer renders the layout defined in `09-wireframe.md`:
  2-column (desktop) / stacked (mobile): "what to do after a [case type]
  accident" plus "what compensation typically covers".
  Fix: Inspect `src/components/CaseTypeExplainer.jsx` against the
  wireframe spec.

- [ ] `casetypeexplainer.brand_dna_paths_canonical`, CaseTypeExplainer
  reads only `pages.services.items[slug].body`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `casetypeexplainer.copy_from_playbook`, body copy resolves from
  `pages.services.items[slug].body` as written by Stage 6 per the niche
  playbook's copy contract.
  Fix: Verify Stage 6 copy-deck output populates this path for every
  case-type slug.

- [ ] `casetypeexplainer.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

### FAQSection (case-type variant)

- [ ] `casetype_faqsection.composition_matches_wireframe`, the case-type
  FAQSection renders the layout defined in `09-wireframe.md`: accordion, 3
  to 5 case-type-specific items.
  Fix: Inspect `src/components/FAQSection.jsx`'s case-type render path
  against the wireframe spec.

- [ ] `casetype_faqsection.brand_dna_paths_canonical`, the case-type
  FAQSection reads only `pages.services.items[slug].faq[]` and
  `copy.faq.*`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `casetype_faqsection.copy_from_playbook`, locked copy strings
  resolve from `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `casetype_faqsection.no_em_dashes`, zero em-dashes in rendered
  output.
  Fix: Replace em-dashes with colons, periods, or commas.

- [ ] `casetype_faqsection.schema_present`, each case-type FAQ block
  emits `FAQPage` JSON-LD schema and the page emits `BreadcrumbList`
  schema.
  Fix: Verify `inject-theme.mjs` injects `FAQPage` and `BreadcrumbList`
  schema for case-type routes.

### CTABand + LeadForm (case-type variant)

- [ ] `casetype_ctaband.composition_matches_wireframe`, the case-type
  CTABand renders the layout defined in `09-wireframe.md`: full-width
  band, accent background, LeadForm variant="page" below, repeating the
  4-field form per `09-template-spec.md` Section 6.
  Fix: Inspect the CTABand instance in
  `src/components/CaseTypeLandingPage.jsx` against the wireframe spec.

- [ ] `casetype_ctaband.brand_dna_paths_canonical`, the case-type CTABand
  reads only `copy.cta.*`, `copy.formHeader`, `copy.buttonText`.
  Fix: Run `npm run validate` and review reported paths.

- [ ] `casetype_ctaband.copy_from_playbook`, locked copy strings resolve
  from `niche-playbook/copy-locks.json` where applicable.
  Fix: Verify the component reads via canonical `brand-dna.copy.*` paths
  Stage 10.1 fills from the playbook.

- [ ] `casetype_ctaband.no_em_dashes`, zero em-dashes in rendered output.
  Fix: Replace em-dashes with colons, periods, or commas.

---

## TRUST STACK ORDER (cross-cutting check)

- [ ] `trust_stack.nine_item_order`, across the site, the 9-item trust
  stack order from `09-template-spec.md` Section 7 is preserved: (1) no
  win, no fee / contingency guarantee near every CTA, (2) settlement-dollar
  proof tagged by case type, (3) press / "as seen in" or local award
  badges, (4) risk-removal "why choose us" bullets, (5) case results grid
  with dollar figures, (6) named attorney story and photo, (7) testimonials
  with photos tied to case type and outcome, (8) FAQ (PAA-driven), (9)
  office locations (multi-office firms only, omitted when
  `brandDNA.location_pages[]` is empty).
  Fix: Walk each page's section order against this list; for single-office
  clients, confirm item 9 is absent (not rendered as an empty section).

---

## LEADFORM CHECKS (CRO-locked, HALT-LEVEL)

The 4-field cap is the niche's CRO deviation from the Morgan & Morgan
reference (which ships 5 fields). This is a HALT-level check: a failure here
halts Stage 10.4b regardless of overall pass rate.

- [ ] `leadform.hero_four_fields` (HALT), the Hero LeadForm instance
  (variant="hero") renders exactly 4 fields: name, phone, email, case
  description. Never more, never fewer.
  Fix: Inspect `src/components/LeadForm.jsx`'s hero variant; remove any
  extra field (e.g. zip code, injury type) and route second-step
  qualification fields to `niche-playbook/cro-rules.md` per
  `09-template-spec.md` Section 6.

- [ ] `leadform.footer_four_fields` (HALT), the Footer LeadForm instance
  (variant="footer") renders exactly 4 fields: name, phone, email, case
  description.
  Fix: Same as `leadform.hero_four_fields`, applied to the footer variant.

- [ ] `leadform.casetype_cta_four_fields` (HALT), the LeadForm instance in
  every case-type page's CTABand (variant="page") renders exactly 4
  fields: name, phone, email, case description.
  Fix: Same as `leadform.hero_four_fields`, applied to the case-type CTA
  variant, checked across all 7 case-type routes.

- [ ] `leadform.contact_page_four_fields` (HALT), the LeadForm instance on
  the Contact page (variant="page") renders exactly 4 fields: name, phone,
  email, case description.
  Fix: Same as `leadform.hero_four_fields`, applied to the contact page
  variant.

- [ ] `leadform.field_order_consistent`, all 4 fields appear in the same
  order (name, phone, email, case description) across every LeadForm
  instance.
  Fix: Align field order in `src/components/LeadForm.jsx` across all
  variants.

---

## CASE-TYPE PAGE COMPLETENESS CHECKS

All 7 routes from `09-sitemap.json`'s `CaseTypeLandingPage` instances:
car-accident, truck-accident, motorcycle-accident, slip-and-fall,
wrongful-death, dog-bite, brain-injury.

- [ ] `casetype_routes.car_accident_resolves`, `/case-types/car-accident`
  renders with a non-empty `pages.services.items['car-accident']` entry
  (name, settlementFigure, body, faq[]).
  Fix: Verify `brandDNA.pages.services.items['car-accident']` is fully
  populated by Stage 6 / Stage 7.

- [ ] `casetype_routes.truck_accident_resolves`,
  `/case-types/truck-accident` renders with a non-empty
  `pages.services.items['truck-accident']` entry.
  Fix: Verify `brandDNA.pages.services.items['truck-accident']` is fully
  populated.

- [ ] `casetype_routes.motorcycle_accident_resolves`,
  `/case-types/motorcycle-accident` renders with a non-empty
  `pages.services.items['motorcycle-accident']` entry.
  Fix: Verify `brandDNA.pages.services.items['motorcycle-accident']` is
  fully populated.

- [ ] `casetype_routes.slip_and_fall_resolves`,
  `/case-types/slip-and-fall` renders with a non-empty
  `pages.services.items['slip-and-fall']` entry.
  Fix: Verify `brandDNA.pages.services.items['slip-and-fall']` is fully
  populated.

- [ ] `casetype_routes.wrongful_death_resolves`,
  `/case-types/wrongful-death` renders with a non-empty
  `pages.services.items['wrongful-death']` entry.
  Fix: Verify `brandDNA.pages.services.items['wrongful-death']` is fully
  populated.

- [ ] `casetype_routes.dog_bite_resolves`, `/case-types/dog-bite` renders
  with a non-empty `pages.services.items['dog-bite']` entry.
  Fix: Verify `brandDNA.pages.services.items['dog-bite']` is fully
  populated.

- [ ] `casetype_routes.brain_injury_resolves`,
  `/case-types/brain-injury` renders with a non-empty
  `pages.services.items['brain-injury']` entry.
  Fix: Verify `brandDNA.pages.services.items['brain-injury']` is fully
  populated.

---

## SITE-WIDE ROUTE CHECKS

Per `09-sitemap.json`, the following 10 route groups must all render
without error: `/`, `/about`, `/practice-areas`, `/case-types/:slug` (7
instances, checked above), `/testimonials`, `/faq`, `/contact`, `/blog`,
`/blog/:slug`, and the `*` not-found route.

- [ ] `routes.about_renders`, `/about` renders PageHero, FounderStory
  (expanded), WhyChooseUs, Testimonials, CTABand.
  Fix: Inspect `src/pages/AboutPage.jsx` against `09-sitemap.json`.

- [ ] `routes.practice_areas_renders`, `/practice-areas` renders
  PageHero, PracticeAreasGrid (all 7 cards), CTABand.
  Fix: Inspect `src/pages/PracticeAreasPage.jsx` against
  `09-sitemap.json`.

- [ ] `routes.testimonials_renders`, `/testimonials` renders PageHero,
  Testimonials (full list, grid not carousel), CTABand.
  Fix: Inspect `src/pages/TestimonialsPage.jsx` against `09-sitemap.json`.

- [ ] `routes.faq_renders`, `/faq` renders PageHero, FAQSection (full
  list), CTABand, with `FAQPage` schema.
  Fix: Inspect `src/pages/FAQPage.jsx` against `09-sitemap.json`.

- [ ] `routes.contact_renders`, `/contact` renders PageHero,
  ContactDetails (address, phone, hours, map), LeadForm (4 fields).
  Fix: Inspect `src/pages/ContactPage.jsx` against `09-sitemap.json`.

- [ ] `routes.blog_renders`, `/blog` renders PageHero and BlogGrid
  (cards: cover, title, excerpt, date, category).
  Fix: Inspect `src/pages/BlogPage.jsx` against `09-sitemap.json`.

- [ ] `routes.blog_post_renders`, `/blog/:slug` renders BlogPostHeader,
  BlogPostBody, CTABand for at least one published post.
  Fix: Inspect `src/pages/BlogPostPage.jsx` against `09-sitemap.json`.

- [ ] `routes.not_found_renders`, the `*` route renders `NotFoundPage`.
  Fix: Inspect `src/pages/NotFoundPage.jsx` and the router's catch-all
  route.

---

## MOBILE CHECKS

- [ ] `mobile.no_horizontal_scroll`, at 375px viewport no section
  overflows horizontally, including the StatsBar's scroll-snap grid (must
  scroll within its own bounds, not the page).
  Fix: Per-section CSS audit.

- [ ] `mobile.touch_targets`, every interactive element (nav links, CALL
  NOW button, LeadForm submit button, FAQ accordion toggles,
  PracticeAreasGrid cards) is at least 44px in tap area.
  Fix: Increase padding / hit-area.

- [ ] `mobile.legible_body_copy`, body copy >= 14px effective, including
  CaseTypeExplainer's stacked body text.
  Fix: Adjust the Tailwind config's body font size.

- [ ] `mobile.sticky_header_call_now`, the sticky header's "CALL NOW" link
  is visible and tappable at all scroll positions on a 390px viewport,
  including when the practice-area mega-nav dropdown or mobile hamburger
  menu is open.
  Fix: Verify Header z-index, sticky positioning, and dropdown/hamburger
  overlay behavior in `src/components/Header.jsx`.

- [ ] `mobile.lead_form_usable_390px`, every LeadForm instance (hero,
  footer, case-type CTA, contact page) is usable on a 390px viewport: all
  4 fields stack into a single column, labels and placeholders are fully
  visible, and the submit button is not clipped or overlapped.
  Fix: Inspect `src/components/LeadForm.jsx` responsive grid classes.

---

## PASS CRITERIA

The merged checklist (this per-niche layer plus the universal layer) must
pass at 95% (PASS / (PASS + FAIL), excluding N/A) with zero em-dash hits and
zero AI-vocab-blocklist hits.

The four `leadform.*_four_fields` checks above are HALT-level: a failure on
any one of them halts Stage 10.4b regardless of the overall pass rate,
because the 4-field LeadForm cap is this niche's locked CRO deviation from
the Morgan & Morgan reference template.
